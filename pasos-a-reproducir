/*****************
composer require laravel/ui
php artisan ui bootstrap --auth
npm install && npm run dev
php artisan ui vue
npm install && npm run dev
npm install vue-progressbar
npm install && npm run dev
php artisan storage:link
npm install --save dropzone
npm install && npm run dev
C:\Users\Andres\inspeccion\resources\js\app.js -> Agregar las siguientes lineas:

    import VueProgressBar from 'vue-progressbar'
    Vue.component('mail-tomador', require('./components/MailTomador.vue').default);
    Vue.component('mail-cia', require('./components/MailCia.vue').default);
    Vue.use(VueProgressBar, {
        color: '#8FFF00',
        failedColor: 'red',
        thickness: '4px',
      })

Usuarios Administradores: type >= 100

*/

